import { UserReadResponse } from '@amadda/global-types';
import { useState } from 'react';

export function usePartyDelete(userSeq, participants) {
  // const [participants, setParticipants] = useState<Array<UserReadResponse>>([]);
  // const deleteParticipant = (userSeq: string) => {
  //   const result = participants.filter(p => p.userSeq !== Number(userSeq));
  //   setParticipants(result);
  // };
}
