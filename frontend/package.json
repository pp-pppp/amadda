{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build --concurrency 15",
    "dev": "dotenv -- turbo run dev --concurrency 15",
    "lint": "turbo run lint --concurrency 15",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "workspace:*",
    "dotenv-cli": "latest",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "amadda"
}
